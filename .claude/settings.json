{
  "permissions": {
    "allow": [
      "PowerShell(pnpm add @capacitor/core @capacitor/app @capacitor/splash-screen 2>&1 | Select-Object -Last 12; Write-Output \"Exit: $LASTEXITCODE\")",
      "PowerShell(pnpm add -D @capacitor/cli @capacitor/android 2>&1 | Select-Object -Last 10; Write-Output \"Exit: $LASTEXITCODE\")",
      "PowerShell(npx next build 2>&1 | Select-Object -Last 25; Write-Output \"Exit: $LASTEXITCODE\")",
      "Bash(taskkill /F /IM node.exe)"
    ]
  }
}
